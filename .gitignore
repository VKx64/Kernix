.env
.env.*
!.env.example
# Template only — the real mcp/.env.production stays on the server.
!mcp/.env.production.example

backend/.env
backend/vendor/
backend/storage/*.key
backend/storage/framework/cache/data/*
backend/storage/framework/sessions/*
backend/storage/framework/views/*
backend/storage/logs/*
frontend/node_modules/
frontend/dist/
mcp/node_modules/
mcp/dist/
mcp/*.tsbuildinfo
whatsapp/node_modules/
whatsapp/dist/
whatsapp/*.tsbuildinfo
# The linked WhatsApp session, if the bridge is ever run outside Docker. These
# files are credentials for a real account and must never be committed.
whatsapp/auth/
# Names the site and its ports; lives on the server only.
mcp/.env.production

# QA work products: findings and screenshots from review passes, kept locally
# rather than in the repo.
visual-qa/
OLIVER-QC.md
mcp/QC.md

*.bak-*
_install*.php

legacy/uploads/*
!legacy/uploads/.htaccess
!legacy/uploads/index.html
