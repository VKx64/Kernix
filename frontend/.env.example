# Leave blank when the browser reaches Laravel through the same reverse proxy.
VITE_API_ORIGIN=

# Used only by the Vite development proxy. In Docker this is the backend service.
VITE_PROXY_TARGET=http://backend:8000
